# This is a tmux tool used to save and recover sessions

## Usage:
```
Usage: tmux-sessions <ACTION>

Arguments:
  <ACTION>  Action to do with the tmux sessions [possible values: save, restore]

Options:
  -h, --help     Print help
  -V, --version  Print version
```
